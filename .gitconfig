[user]
	email = hamster.of.dev@gmail.com
	name = Hans Meyer

[core]
    editor = vim

[alias]
	today = !git log --graph --pretty=log --since=\"yesterday\" --author=\"$(git config user.name)\" --color
    co = checkout

[help]
	autocorrect = true

[color]
	branch = auto

[push]
	default = simple

[pretty]
	log = format:SHA1: %h%nAuthor: %an <%ae>%nCommiter: %cn <%ce%>%nDate: %cd%n%n%s%n
